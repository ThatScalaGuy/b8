# Revisions listed here are skipped by `git blame`.
# Enable once per clone:  git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Bulk-reformatting commits (e.g. "Scala Steward: Reformat with scalafmt X.Y.Z")
# belong in this file — they touch every line without changing behaviour.
