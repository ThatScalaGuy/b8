# Laika treats a directory's `README.md` as its title document by default; b8
# keeps its landing page in `index.md`, which is what the site's home link and
# navigation root point at.
laika.titleDocuments.inputName = index
